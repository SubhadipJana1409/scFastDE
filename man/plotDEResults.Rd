% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotDEResults.R
\name{plotDEResults}
\alias{plotDEResults}
\title{Volcano Plot of Differential Expression Results}
\usage{
plotDEResults(
  result,
  fdr_thresh = 0.05,
  lfc_thresh = 1,
  top_n = 10L,
  point_size = 1,
  point_alpha = 0.6,
  colours = c(up = "#E24B4A", down = "#378ADD", ns = "#888780")
)
}
\arguments{
\item{result}{A \code{\link{FDEResult}} object from \code{\link{fastDE}}.}

\item{fdr_thresh}{A \code{numeric(1)} FDR threshold for significance.
Default: \code{0.05}.}

\item{lfc_thresh}{A \code{numeric(1)} absolute log2 fold-change
threshold. Default: \code{1}.}

\item{top_n}{An \code{integer(1)} number of top significant genes to
label by name. Default: \code{10}.}

\item{point_size}{A \code{numeric(1)} point size. Default: \code{1}.}

\item{point_alpha}{A \code{numeric(1)} point transparency. Default:
\code{0.6}.}

\item{colours}{A named \code{character} vector of colours for
\code{"up"}, \code{"down"}, and \code{"ns"} (not significant).
Default: \code{c(up = "#E24B4A", down = "#378ADD", ns = "#888780")}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Produces a volcano plot from a \code{\link{FDEResult}} object, with
genes coloured by significance and optionally labelled. Points are
coloured by whether they exceed the \code{lfc_thresh} and
\code{fdr_thresh} thresholds.
}
\examples{
library(SingleCellExperiment)

set.seed(42)
n_genes <- 200
counts <- matrix(rpois(n_genes * 60, 8), nrow = n_genes, ncol = 60)
rownames(counts) <- paste0("Gene", seq_len(n_genes))
colnames(counts) <- paste0("Cell", seq_len(60))
counts[1:10, 31:60] <- counts[1:10, 31:60] * 3L

sce <- SingleCellExperiment(assays = list(counts = counts))
sce$donor     <- rep(paste0("D", 1:6), each = 10)
sce$cell_type <- "Tcell"
sce$condition <- rep(c("ctrl", "treat"), each = 30)

result <- fastDE(sce, donor = "donor", cell_type = "cell_type",
                 condition = "condition", target_type = "Tcell",
                 min_cells = 5)
plotDEResults(result)

}
\seealso{
\code{\link{fastDE}}, \code{\link{FDEResult}}
}
